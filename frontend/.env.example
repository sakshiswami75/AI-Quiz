# The frontend calls the backend via the relative path /api (proxied by Vite).
# You normally don't need to change anything here.

# Vite dev-server proxy target (the Express backend)
VITE_API_PROXY_TARGET=http://127.0.0.1:5000
